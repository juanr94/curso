<!--<p id="notice"><%= notice %></p>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Categoria</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.categoria %></td>
        <td><%= book.year %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>-->
<h1>Books</h1>
<p id="notice">
  <%= notice %>
</p>

<main>
  <section class="py-1 text-center container">
    <div class="row py-lg-1">
      <div class="col-lg-6 col-md-8 mx-auto">
        <h1 class="fw-light">Libros Disponibles</h1>
      </div>
    </div>
  </section>

  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% @books.each do |books| %>
          <div class="col">
            <div class="card shadow-sm border-primary">
                <div class="card-body">
                  <p class="card-text">
                    <h3>
                      <%= books.title %>
                    </h3>
                    <span><%= books.categoria %></span>
                    <br>
                    <span><%= books.year %></span>
                  </p>
                </div>
                <div><%= link_to 'Show', books %> | <%= link_to 'Edit', edit_book_path(books) %> | <%= link_to 'Destroy', books, method: :delete, data: { confirm: 'Are you sure?' } %></div>
            </div>
          </div>
          <tr>
      </tr>
          <% end %>
      </div>

    </div>
      <%= link_to 'New Book', new_book_path %>
  </div>
</main>
